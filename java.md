Add java integration

If I rmb pom.xml I should be able to load project as maven project, that should mark project as java project and save it in the workspace options

For java projects I should be able to 
1. See project options (json is fine, no ui), should include file sources and idk, mvn paths
2. Add folders to sources e.g. src/generated by right clicking folder -> add as source
3. Get a project view, that is not file structure based but package based (same as idea)
4. Expandable java panel on the bottom, with build/run/stop buttons on the left and log

