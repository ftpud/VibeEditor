{
  "id": "7bc7c839-d765-4f5a-a075-46f56356adf7",
  "name": "3 Concurrent Tasks + 30s Wait + Summary",
  "version": 2,
  "createdAt": "2026-09-17T00:00:00.000Z",
  "updatedAt": "2026-09-17T00:00:00.000Z",
  "blocks": [
    {
      "id": "dispatcher",
      "type": "prompt",
      "label": "Dispatch 3 concurrent tasks",
      "prompt": "You are a short-lived dispatcher. The user request is:\n\n{{input}}\n\nImmediately call workflow_run_stack exactly once on the connected Start task block. Pass exactly three distinct, self-contained prompts. Every prompt must include the complete user request above and identify itself as task 1, 2, or 3. Split the work into useful parallel responsibilities when possible. The downstream block only starts each task and returns immediately, so the three task workspaces run concurrently. As soon as workflow_run_stack reports that all three tasks were started, return the tool result and end this block. Do not monitor, merge, or wait for the tasks here.",
      "provider": "codex",
      "model": "gpt-5.6-terra",
      "position": { "x": 30, "y": 40 }
    },
    {
      "id": "task-starter",
      "type": "task",
      "label": "Start task",
      "prompt": "This is stack item {{iteration}}. Input:\n\n{{input}}\n\nImmediately call task_create_and_start exactly once with provider codex and model gpt-5.6-terra to create an isolated implementation workspace for this input. As soon as the tool returns, report the created task id and return immediately. Never poll, wait, merge, or execute the implementation in this block. Do not wait for previously started tasks. This immediate return is required so all three created task workspaces keep working concurrently.",
      "provider": "codex",
      "model": "gpt-5.6-terra",
      "position": { "x": 300, "y": 40 }
    },
    {
      "id": "task-watcher",
      "type": "task",
      "label": "Wait for all 3 tasks",
      "prompt": "The three task starters have returned. Their outputs, including the three task ids, are below:\n\n{{input}}\n\nExtract all three task ids. Monitor all three concurrently using task_list and task_ai_response_tail. If any task is still running or waiting, call timer_set for a short polling delay and continue this same persistent session when revived. Do not create replacement or duplicate tasks. Once every task has completed, collect each final response. Merge each successful task with task_merge using strategy smart; merge calls may be issued together because the runtime serializes them safely. Return one complete report containing every task id, completion status, final response, and merge result. Do not call workflow_run_stack.",
      "provider": "codex",
      "model": "gpt-5.6-terra",
      "position": { "x": 570, "y": 40 }
    },
    {
      "id": "timed-worker",
      "type": "prompt",
      "label": "30-second worker",
      "prompt": "The user request is:\n\n{{input}}\n\nOn the first turn, call timer_set once with exactly 30 seconds. Its continuation prompt must tell you that the timer elapsed and include the complete user request above. Do not execute the request before setting the timer and do not set another timer after revival. When the continuation arrives, execute or analyze the user request and return your result. Do not call workflow_run_stack; the graph will release the Summary block when both incoming paths finish.",
      "provider": "codex",
      "model": "gpt-5.6-terra",
      "position": { "x": 300, "y": 300 }
    },
    {
      "id": "summary",
      "type": "prompt",
      "label": "Summarize all outputs",
      "prompt": "All three concurrent implementation tasks and the independent 30-second branch are complete. Produce a concise but complete final response for the user from every output below. Clearly distinguish the three task results, their merge outcomes, and the 30-second result. Mention failures or missing outputs explicitly. Do not start more tasks and do not call workflow_run_stack.\n\n{{input}}",
      "join": "all",
      "provider": "codex",
      "model": "gpt-5.6-terra",
      "position": { "x": 850, "y": 170 }
    }
  ],
  "edges": [
    { "id": "dispatcher-to-starter", "from": "dispatcher", "to": "task-starter", "label": "three implementation prompts" },
    { "id": "starter-to-watcher", "from": "task-starter", "to": "task-watcher", "label": "three task ids" },
    { "id": "watcher-to-summary", "from": "task-watcher", "to": "summary", "label": "task results" },
    { "id": "timer-to-summary", "from": "timed-worker", "to": "summary", "label": "timed result" }
  ]
}
